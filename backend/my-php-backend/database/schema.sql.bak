-- ========================================================================
-- MHACTO Optimized Database Schema (mhacto_db)
-- Bocaue, Bulacan — Tourism Website
-- Optimized: 17 → 10 tables  (41 % reduction)
-- ========================================================================
--
-- MERGED / ELIMINATED
-- ───────────────────────────────────────────────────────────────────
--  OLD TABLE           →  WHERE IT WENT
--  ─────────────       ─  ───────────────────────────────────────
--  User                →  users              (renamed)
--  catergory           →  categories         (typo fixed, merged with content_label)
--  content_label       →  categories         (merged via cat_type = 'label')
--  cms                 →  content            (absorbs place + news columns)
--  place               →  content            (columns inlined)
--  news                →  content            (news_date inlined)
--  content_image       →  content_images     (renamed)
--  spotlight           →  featured_content   (merged with landmarks)
--  featured_landmarks  →  featured_content   (merged with spotlight)
--  hero_slides         →  DROPPED            (single hero → site_settings)
--  hero_settings       →  site_settings      (hero columns absorbed)
--  culinary_items      →  DROPPED            (auto-pulled from CMS label)
--  click_analytics     →  activity_logs      (merged, action = 'page_view')
--  inquiry_sender      →  inquiries          (sender columns inlined)
--  student_verify      →  inquiries          (student columns inlined)
--  customer_inquiries  →  inquiries          (renamed + absorbed sender/student)
--
-- FINAL TABLES (10)
-- ───────────────────────────────────────────────────────────────────
--  1. users
--  2. categories         (category + label, hierarchical)
--  3. content            (flat CMS: place + news + event)
--  4. content_images     (1:many images per content)
--  5. featured_content   (homepage spotlight & landmark selections)
--  6. inquiries          (visitor inquiries, self-contained)
--  7. visit_purposes     (lookup for inquiry purpose dropdown)
--  8. milestones         (history timeline, unique structure)
--  9. site_settings      (single-row config + hero settings)
-- 10. activity_logs      (admin audit + public page views)
-- ========================================================================


-- ────────────────────────────────────────────────────────────────
-- 1. USERS  (was `User`)
-- ────────────────────────────────────────────────────────────────
CREATE TABLE users (
  user_id       INT AUTO_INCREMENT PRIMARY KEY,
  username      VARCHAR(100) DEFAULT NULL,
  email         VARCHAR(255) NOT NULL,
  password_hash VARCHAR(100) DEFAULT NULL,
  role          VARCHAR(20)  DEFAULT 'admin',
  created_at    TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uq_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 2. CATEGORIES  (merges `catergory` + `content_label`)
--    cat_type='category'  → broad groups (History, Arts & Culture …)
--    cat_type='label'     → sub-labels  (local-cuisine, festivals …)
--    parent_id            → optional hierarchy (label → parent category)
-- ────────────────────────────────────────────────────────────────
CREATE TABLE categories (
  category_id INT AUTO_INCREMENT PRIMARY KEY,
  parent_id   INT          DEFAULT NULL   COMMENT 'FK to parent category (for labels)',
  cat_type    ENUM('category','label') DEFAULT 'category',
  label_key   VARCHAR(50)  DEFAULT NULL   COMMENT 'URL-safe key (labels only)',
  label_name  VARCHAR(50)  NOT NULL       COMMENT 'Display name',
  color_code  VARCHAR(50)  DEFAULT NULL   COMMENT 'Badge colour (categories only)',
  is_active   TINYINT(1)   DEFAULT 1,
  FOREIGN KEY (parent_id) REFERENCES categories(category_id) ON DELETE SET NULL,
  INDEX idx_type      (cat_type),
  INDEX idx_label_key (label_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 3. CONTENT  (merges `cms` + `place` + `news`)
--    Place-only columns are NULL when post_type ≠ 'place'
--    news_date is NULL when post_type ≠ 'news'/'event'
-- ────────────────────────────────────────────────────────────────
CREATE TABLE content (
  content_id     INT AUTO_INCREMENT PRIMARY KEY,
  user_id        INT          DEFAULT NULL,
  category_id    INT          DEFAULT NULL  COMMENT 'FK → categories (type=category)',
  label_id       INT          DEFAULT NULL  COMMENT 'FK → categories (type=label)',
  title          VARCHAR(255) DEFAULT NULL,
  description    TEXT         DEFAULT NULL,
  status         ENUM('draft','published','archived') DEFAULT 'draft',
  post_type      ENUM('place','news','event') DEFAULT 'place',
  is_featured    TINYINT(1)   DEFAULT 0,
  -- Place-specific (NULL when not a place)
  location       VARCHAR(255) DEFAULT NULL,
  hours          VARCHAR(100) DEFAULT NULL,
  visit_date     DATETIME     DEFAULT NULL,
  contact        VARCHAR(100) DEFAULT NULL,
  established    VARCHAR(50)  DEFAULT NULL,
  place_category VARCHAR(100) DEFAULT NULL,
  story          TEXT         DEFAULT NULL,
  -- News/Event-specific (NULL when not news/event)
  news_date      DATE         DEFAULT NULL,
  created_at     TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
  updated_at     TIMESTAMP    DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id)     REFERENCES users(user_id)          ON DELETE SET NULL,
  FOREIGN KEY (category_id) REFERENCES categories(category_id) ON DELETE SET NULL,
  FOREIGN KEY (label_id)    REFERENCES categories(category_id) ON DELETE SET NULL,
  INDEX idx_status    (status),
  INDEX idx_post_type (post_type),
  INDEX idx_featured  (is_featured)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 4. CONTENT_IMAGES  (was `content_image`)
-- ────────────────────────────────────────────────────────────────
CREATE TABLE content_images (
  image_id     INT AUTO_INCREMENT PRIMARY KEY,
  content_id   INT          DEFAULT NULL,
  image_url    VARCHAR(255) DEFAULT NULL,
  is_thumbnail TINYINT(1)   DEFAULT 0,
  sort_order   INT          DEFAULT 0,
  created_at   TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (content_id) REFERENCES content(content_id) ON DELETE CASCADE,
  INDEX idx_content (content_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 5. FEATURED_CONTENT  (merges `spotlight` + `featured_landmarks`)
--    Points to a CMS content row — no data duplication.
-- ────────────────────────────────────────────────────────────────
CREATE TABLE featured_content (
  featured_id INT AUTO_INCREMENT PRIMARY KEY,
  content_id  INT          DEFAULT NULL  COMMENT 'FK → content.content_id',
  section     ENUM('spotlight','landmark') NOT NULL,
  sort_order  INT          DEFAULT 0,
  is_active   TINYINT(1)   DEFAULT 1,
  created_at  TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
  updated_at  TIMESTAMP    DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (content_id) REFERENCES content(content_id) ON DELETE CASCADE,
  INDEX idx_section_active (section, is_active, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 6. INQUIRIES  (merges `customer_inquiries` + `inquiry_sender`
--                + `student_verify`)
-- ────────────────────────────────────────────────────────────────
CREATE TABLE inquiries (
  inquiry_id     INT AUTO_INCREMENT PRIMARY KEY,
  -- Sender info (was inquiry_sender)
  full_name      VARCHAR(255) DEFAULT NULL,
  email_address  VARCHAR(255) DEFAULT NULL,
  contact_number VARCHAR(255) DEFAULT NULL,
  subject        VARCHAR(255) DEFAULT NULL,
  -- Inquiry body
  inquiry_type   INT          DEFAULT 1   COMMENT '1=general, 2=student, etc.',
  purpose_id     INT          DEFAULT NULL,
  date_of_visit  DATE         DEFAULT NULL,
  number_of_pax  INT          DEFAULT NULL,
  message        TEXT         DEFAULT NULL,
  is_read        TINYINT(1)   DEFAULT 0,
  is_assigned    TINYINT(1)   DEFAULT 0,
  folder         ENUM('inbox','archive','spam','trash') DEFAULT 'inbox',
  -- Student verification (was student_verify — nullable)
  student_number VARCHAR(255) DEFAULT NULL,
  school_name    VARCHAR(255) DEFAULT NULL,
  reply_message  TEXT         DEFAULT NULL,
  replied_at     TIMESTAMP    NULL DEFAULT NULL,
  created_at     TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
  deleted_at     TIMESTAMP    NULL DEFAULT NULL,
  FOREIGN KEY (purpose_id) REFERENCES visit_purposes(purpose_id) ON DELETE SET NULL,
  INDEX idx_folder (folder),
  INDEX idx_read   (is_read)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 7. VISIT_PURPOSES  (unchanged — admin-managed lookup)
-- ────────────────────────────────────────────────────────────────
CREATE TABLE visit_purposes (
  purpose_id   INT AUTO_INCREMENT PRIMARY KEY,
  purpose_name VARCHAR(100) DEFAULT NULL,
  is_active    TINYINT(1)   DEFAULT 1
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 8. MILESTONES  (unchanged — unique timeline structure)
-- ────────────────────────────────────────────────────────────────
CREATE TABLE milestones (
  milestone_id INT AUTO_INCREMENT PRIMARY KEY,
  year         VARCHAR(50)  NOT NULL,
  title        VARCHAR(255) NOT NULL,
  description  TEXT         DEFAULT NULL,
  detail       TEXT         DEFAULT NULL,
  side         ENUM('left','right') DEFAULT 'left',
  sort_order   INT          DEFAULT 0,
  is_active    TINYINT(1)   DEFAULT 1,
  created_at   TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
  updated_at   TIMESTAMP    DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_active_order (is_active, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 9. SITE_SETTINGS  (absorbs hero_settings — single-row config)
-- ────────────────────────────────────────────────────────────────
CREATE TABLE site_settings (
  settings_id       INT AUTO_INCREMENT PRIMARY KEY,
  last_updated_by   INT          DEFAULT NULL,
  site_name         VARCHAR(100) DEFAULT NULL,
  site_description  TEXT         DEFAULT NULL,
  contact_email     VARCHAR(100) DEFAULT NULL,
  contact_phone     VARCHAR(50)  DEFAULT NULL,
  office_address    VARCHAR(255) DEFAULT NULL,
  site_logo_url     VARCHAR(255) DEFAULT NULL,
  notify_inquiries  TINYINT(1)   DEFAULT 1,
  enable_analytics  TINYINT(1)   DEFAULT 1,
  -- Hero section (absorbed from hero_settings)
  hero_subtitle     VARCHAR(100) DEFAULT '',
  hero_title        VARCHAR(100) DEFAULT 'Explore The River',
  hero_highlight    VARCHAR(100) DEFAULT '',
  hero_description  TEXT         DEFAULT NULL,
  hero_video_url    VARCHAR(500) DEFAULT '',
  hero_fallback_img VARCHAR(500) DEFAULT '',
  hero_cta_text     VARCHAR(100) DEFAULT 'Explore Now',
  hero_cta_link     VARCHAR(255) DEFAULT '/destinations',
  updated_at        TIMESTAMP    DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (last_updated_by) REFERENCES users(user_id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ────────────────────────────────────────────────────────────────
-- 10. ACTIVITY_LOGS  (absorbs `click_analytics`)
--     Admin actions:  create, update, delete, login, logout
--     Public traffic: page_view  (was click_analytics)
-- ────────────────────────────────────────────────────────────────
CREATE TABLE activity_logs (
  log_id     INT AUTO_INCREMENT PRIMARY KEY,
  user_id    INT          DEFAULT NULL  COMMENT 'NULL for public page_view events',
  content_id INT          DEFAULT NULL  COMMENT 'Related CMS content (optional)',
  action     ENUM('create','update','delete','login','logout','page_view') DEFAULT NULL,
  details    TEXT         DEFAULT NULL,
  page_path  VARCHAR(255) DEFAULT NULL  COMMENT 'URL path (page_view events)',
  ip_address VARCHAR(45)  DEFAULT NULL,
  created_at TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE SET NULL,
  INDEX idx_action  (action),
  INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


-- ========================================================================
-- SEED DATA
-- ========================================================================

-- Default admin user (password: admin123)
INSERT INTO users (username, email, password_hash, role) VALUES
('admin', 'mhacto.municipalityofbocaue@gmail.com', '$2y$12$bAccO9YaDEfSb0HO/TT5aeEZY3ehXExHqrYUPcxugQffxk5U7BmLG', 'admin');

-- Categories (broad groups)
INSERT INTO categories (cat_type, label_key, label_name, color_code, is_active) VALUES
('category', NULL, 'History',               '#3b82f6', 1),
('category', NULL, 'Arts & Culture',        '#10b981', 1),
('category', NULL, 'Tourist Destinations',  '#f59e0b', 1),
('category', NULL, 'News & Events',         '#ef4444', 1);

-- Labels (sub-labels, linked to parent category)
INSERT INTO categories (parent_id, cat_type, label_key, label_name, is_active) VALUES
(1, 'label', 'timeline-of-events',  'Timeline of Events',  1),
(1, 'label', 'notable-figures',     'Notable Figures',     1),
(2, 'label', 'local-cuisine',       'Local Cuisine',       1),
(2, 'label', 'festivals',           'Festivals',           1),
(2, 'label', 'cultural-practices',  'Cultural Practices',  1),
(3, 'label', 'destinations',        'Destinations',        1),
(3, 'label', 'travel-tours',        'Travel Tours',        1),
(4, 'label', 'events',              'Events',              1),
(4, 'label', 'news',                'News',                1);

-- Community category + labels
INSERT INTO categories (cat_type, label_key, label_name, color_code, is_active) VALUES
('category', NULL, 'Community', '#6366f1', 1);

SET @community_id = LAST_INSERT_ID();

INSERT INTO categories (parent_id, cat_type, label_key, label_name, is_active) VALUES
(@community_id, 'label', 'schools',     'Schools',     1),
(@community_id, 'label', 'colleges',    'Colleges',    1),
(@community_id, 'label', 'hospitals',   'Hospitals',   1),
(@community_id, 'label', 'bocauenos',   'Bocauenos',   1);

-- Additional Arts & Culture labels
INSERT INTO categories (parent_id, cat_type, label_key, label_name, is_active)
SELECT 2, 'label', 'crafts-artisan', 'Crafts & Artisan', 1
FROM DUAL
WHERE NOT EXISTS (SELECT 1 FROM categories WHERE label_key = 'crafts-artisan');

INSERT INTO categories (parent_id, cat_type, label_key, label_name, is_active)
SELECT 2, 'label', 'people-wonders', 'People & Wonders', 1
FROM DUAL
WHERE NOT EXISTS (SELECT 1 FROM categories WHERE label_key = 'people-wonders');

-- Default site settings (with hero defaults)
INSERT INTO site_settings
  (site_name, site_description, contact_email, contact_phone, office_address,
   hero_subtitle, hero_title, hero_highlight, hero_description,
   hero_video_url, hero_fallback_img, hero_cta_text, hero_cta_link)
VALUES
  ('MHACTO Bocaue',
   'Municipal History, Arts, Culture & Tourism Office — Bocaue, Bulacan',
   'mhacto@bocaue.gov.ph', '(044) 123-4567',
   'Municipal Hall, Bocaue, Bulacan 3018',
   'Bocaue, Bulacan', 'Explore The River', 'Town Wonders',
   'Where rich heritage meets vibrant culture — explore centuries of tradition, lively festivals, and the warm hospitality of Bocaue.',
   '/videos/bocaue-hero.mp4', '/images/heroes/hero-bocaue.jpg',
   'Explore Now', '/destinations');

-- Default visit purposes
INSERT INTO visit_purposes (purpose_name, is_active) VALUES
('Educational Tour',   1),
('Tourism Visit',      1),
('Research',           1),
('Cultural Immersion', 1),
('Other',              1);
